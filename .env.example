# AI Todo List - Environment Variables
# This app stores all user data locally in your browser (IndexedDB).
# No server-side storage or accounts required.
#
# API keys are stored client-side in IndexedDB.
# Configure your API key in the app's Settings page after launching.
#
# For deployment (optional):
# NEXT_PUBLIC_APP_URL=https://your-domain.com
