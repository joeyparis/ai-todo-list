TASK 8: Stabilize initialMessages useMemo Dependency
=====================================================

OBJECTIVE:
Prevent unnecessary recomputation of initialMessages when useLiveQuery returns
a new array reference with identical message content.

SOLUTION APPLIED:
Added persistedMessagesRef to track previous message IDs and only recompute
when IDs actually change, not just when array reference changes.

CODE CHANGES:
File: src/components/chat/ChatPanel.tsx
Lines: 52-63

Added:
  const persistedMessagesRef = useRef<typeof persistedMessages>(undefined)
  const initialMessages = useMemo(() => {
    if (!persistedMessages) return []
    // Only recompute when message IDs actually changed (not just new array reference)
    const prevIds = persistedMessagesRef.current?.map(m => m.id).join(',') ?? ''
    const currIds = persistedMessages.map(m => m.id).join(',')
    if (prevIds === currIds && persistedMessagesRef.current !== undefined) {
      return (persistedMessagesRef.current ?? []).map(toUIMessage)
    }
    persistedMessagesRef.current = persistedMessages
    return persistedMessages.map(toUIMessage)
  }, [persistedMessages])

VERIFICATION RESULTS:
✓ Tests: 61 passed (all tests pass)
✓ Build: Compiled successfully in 1439ms
✓ Code: persistedMessagesRef properly declared and used
✓ Pattern: hasHydratedRef unchanged (lines 125-133)
✓ Logic: initialMessages still used correctly in useChat (line 89)
✓ Hydration: setMessages(initialMessages) still called in useEffect (line 131)

BEHAVIOR:
- When useLiveQuery returns new array reference with same message IDs:
  → prevIds === currIds → returns cached result from previous render
  → No unnecessary recomputation
  
- When message IDs actually change:
  → prevIds !== currIds → recomputes and updates persistedMessagesRef
  → Proper update to initialMessages

COMPLEMENTARY PATTERNS:
- hasHydratedRef: Prevents re-hydration of chat state after initial load
- persistedMessagesRef: Prevents unnecessary memo recomputation on DB writes
- Both patterns work together for optimal performance

STATUS: COMPLETE
